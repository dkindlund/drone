<dl>
<% columns.each :for => @record do |column| %>
  <dt class="<%= "client_#{column.name}_name" %>"><%= column.label -%></dt>
  <dd class="<%= "client_#{column.name}_value" %>"><%= show_column_value(@record, column) -%></dd>
<% end -%>
  <dt></dt>
  <dd><%= render :active_scaffold => 'urls', :constraints => { :client_id => @record.id } %></dd>
</dl>
